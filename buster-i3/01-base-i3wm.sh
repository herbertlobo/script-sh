#!/usr/bin/env bash

# intalação i3-wm
sudo apt install xorg i3-wm i3blocks suckless-tools j4-dmenu-desktop -y
# montra todas as janelas 
sudo apt install rofi -y
# gerencia varias telas
sudo apt install arandr -y
# gerenciador de Arquivos
sudo apt install pcmanfm -y
# desconpactadores
sudo apt install p7zip-full rar unrar gvfs-backends gvfs-fuse xarchiver -y
# Terminal
sudo apt install xfce4-terminal -y
# captura de tels
sudo apt install xfce4-screenshooter -y
# gerenciador de notificações
sudo apt install xfce4-notifyd -y
# Definir papel de parede
sudo apt install feh -y
# Conexção com internet Wifi pelo terminal
sudo apt install wicd-gtk wicd-curses wicd-cli -y
# Extenção: calencario i3-blocks, compissição de tela, numclok ligado, informaçã do systema
sudo apt install gsimplecal compton numlockx inxi -y 
# Editor IDE de Scripts
sudo apt install geany vim -y
# Automatizar janelas teclado mause
sudo apt install wmctrl xdotool xinput lxinput -y
# Temas gtk
sudo apt install lxappearance papirus-icons-theme arc-theme numix-gtk-theme numixi-icon-theme numix-icon-theme-circle brezze-cursor-theme -y
# font Arial
sudo apt install ttf-mscorefonts-installer -y
#informações do systema
sudo apt install neofetch htop -y
# gerenciador de pacote instalador de pacote DEB 
sudo apt install synaptic gdebi -y
# gerenciador de autenticação
sudo apt install policykit-1-gnome -y
# App Audio e controlado
sudo apt install pulseaudio pavucontrol -y
# cria interface grafica pra inscript
sudo apt install yad -y
