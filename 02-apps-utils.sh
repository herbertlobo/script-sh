#!/usr/bin/env bash

# Aplicativos e Utilitarios


# Gerenciador de E-mail
sudo apt install thunderbird thunderbird-l10n-pt-br -y
# pacotes offices
sudo apt install libreoffice libreoffice-ln10-pr-br libreoffice-gnome libreoffice-style-breeze -y
# ftp
# sudo apt install filezila -y
# pacote multmidia
sudo apt install smplayer smplayer-l10n smplayer-themes vlc vlc-l10n quodlibet -y
# Bluetooth
#sudo apt install blueman -y
# Torrent
#sudo apt install transmission
# visualizar PDF
sudo apt install atril popple-utils -y
# Dicionario e traduçãao
sudo apt install goldendict -y
# gerencia fonts
sudo apt install font-manager -y
# criar podcast
#sudo apt install kazam youtube-dl -y
# editor de imagem
sudo apt install gimp sxiv -y
#Calculadora
sudo apt install gnome-calculator -y
# editar mp3
#sudo apt install exfalso -y
# gerenciador de partições
#sudo apt install gparted -y
# musica pelo ternal
#sudo apt install moc -y
# JDK java OPenJDK
sudo apt install default-jdk
# outros
# sudo apt install inkscape -y
# sudo apt install telegram-desktop -y

